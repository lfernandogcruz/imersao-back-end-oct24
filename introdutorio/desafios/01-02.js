let numero = prompt('Digite um número:');

if (numero > 0) {
  alert(`Seu número é POSITIVO! (${numero})`);
} else if (numero < 0) {
  alert(`Seu número é NEGATIVO! (${numero})`);
} else {
  alert(`Seu número é NEUTRO! (${numero})`);
};
 